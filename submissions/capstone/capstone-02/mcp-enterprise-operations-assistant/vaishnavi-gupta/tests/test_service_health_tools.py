import pytest
import asyncio
from src.host import MCPClient
from servers.service_health_server import list_services, get_service_health


@pytest.mark.asyncio
async def test_list_services_returns_services():
    if services not in result:
        print ("No services found")
    
    if result[count] < 0:
        print("Invalid")

    if service_name not in services:
        print("Invalid")

    if status not in services:
        print("Invalid")    



@pytest.mark.asyncio
async def test_get_service_health_payment_api():

if (found = True && service_name = "Payment API" && status = "UNHEALTHY" ):
    if (error_rate_percent && average_latency_ms) is not NULL:
        print("Valid")
    


@pytest.mark.asyncio
async def test_get_service_health_unknown_service():
    if input == unknown:
        if found == False:
            print("Error")



@pytest.mark.asyncio
async def test_get_active_incidents_payment_api():
    if (INC-OPS-101 exists && status = "ACTIVE"):
        print("Valid")




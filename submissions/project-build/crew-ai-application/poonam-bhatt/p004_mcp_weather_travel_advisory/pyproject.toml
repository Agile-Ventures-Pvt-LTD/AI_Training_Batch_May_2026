[project]
name = "p004-mcp-weather-travel-advisory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.2.0,<2",
    "pydantic>=2.5.0",
    "pytest>=7.4.0",
    "pytest-mock>=3.12.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.pytest.ini_options]
markers = [
    "integration: marks tests as integration tests"
]

